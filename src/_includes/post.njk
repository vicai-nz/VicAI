<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="{{ excerpt or title }}">
  <meta name="theme-color" content="#BBFF00">
  <meta property="og:title" content="{{ title }}">
  <meta property="og:description" content="{{ excerpt or title }}">
  <meta property="og:url" content="{{ site.url }}{{ page.url }}">
  <meta property="og:type" content="article">
  <meta name="twitter:card" content="summary_large_image">
  <title>{{ title }} | {{ site.name }} Blog</title>
  <link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><rect fill='%23BBFF00' width='100' height='100'/><text x='50' y='68' font-size='50' font-family='monospace' font-weight='bold' text-anchor='middle' fill='%231A1A1A'>V</text></svg>">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;700&family=Space+Grotesk:wght@500;700&family=Space+Mono:wght@400;700&display=swap" rel="stylesheet">
  <link rel="alternate" type="application/atom+xml" title="{{ site.name }} Blog Feed" href="/feed.xml">
  <link rel="stylesheet" href="https://unpkg.com/aos@2.3.4/dist/aos.css">
  {# Core CSS #}
  {% css %}{% include "css/reset.css" %}{% endcss %}
  {% css %}{% include "css/typography.css" %}{% endcss %}
  {% css %}{% include "css/layout.css" %}{% endcss %}
  {% css %}{% include "css/nav.css" %}{% endcss %}
  {% css %}{% include "css/buttons.css" %}{% endcss %}
  {% css %}{% include "css/cards.css" %}{% endcss %}
  {% css %}{% include "css/footer.css" %}{% endcss %}
  {% css %}{% include "css/a11y.css" %}{% endcss %}
  {# Post-specific CSS #}
  {% css %}{% include "css/blog-post.css" %}{% endcss %}
  {% css %}{% include "css/blog-cards.css" %}{% endcss %}
  <link rel="stylesheet" href="{% getBundleFileUrl 'css' %}">
</head>
<body class="page-blog-post">
  {% include "nav.njk" %}

  <main id="main-content">
    <article class="post">
      <header class="post__header section">
        <div class="container container--narrow">
          <a href="/blog/" class="post__back">&larr; Back to Blog</a>
          <h1 class="post__title" data-aos="fade-up">{{ title }}</h1>
          <div class="post__meta" data-aos="fade-up" data-aos-delay="100">
            <span class="post__author">{{ author }}</span>
            <time class="post__date" datetime="{{ date | dateISO }}">{{ date | dateDisplay }}</time>
            <span class="post__reading-time">{{ content | timeToRead }}</span>
          </div>
          {% if tags %}
          <div class="post__tags" data-aos="fade-up" data-aos-delay="200">
            {% for tag in tags %}
            <span class="tag">{{ tag }}</span>
            {% endfor %}
          </div>
          {% endif %}
        </div>
      </header>
      <div class="post__content container container--narrow">
        {{ content | safe }}
      </div>
    </article>

    <section class="section" data-aos="fade-up">
      <div class="container container--narrow">
        <h2 class="section__label">More from the blog</h2>
        <div class="grid grid--auto">
          {% set posts = collections.blog %}
          {% for post in posts | limit(3) %}
          {% if post.data.title != title %}
          {% include "blog-card.njk" %}
          {% endif %}
          {% endfor %}
        </div>
      </div>
    </section>
  </main>

  {% include "footer.njk" %}

  <script src="https://unpkg.com/aos@2.3.4/dist/aos.js"></script>
  {% js %}{% include "js/nav.js" %}{% endjs %}
  {% js %}{% include "js/active-nav.js" %}{% endjs %}
  {% js %}{% include "js/init.js" %}{% endjs %}
  <script src="{% getBundleFileUrl 'js' %}" defer></script>
</body>
</html>
